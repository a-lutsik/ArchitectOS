{
  "name": "architectos-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Tauri 2 shell for ArchitectOS (Python sidecar + webview).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
